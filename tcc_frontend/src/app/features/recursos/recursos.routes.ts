import { Routes } from '@angular/router';
import { RecursosComponent } from './recursos.component';

export const rotasRecursos: Routes = [{ path: '', component: RecursosComponent, title: 'Recursos de mindfulness | Mindly' }];
