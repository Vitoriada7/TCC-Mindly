import { Routes } from '@angular/router';
import { PerfilComponent } from './perfil.component';

export const rotasPerfil: Routes = [
  { path: '', component: PerfilComponent, title: 'Perfil | Mindly' },
];
